import type { BlogPost } from "@/lib/types";

/**
 * Conteúdo do site — fonte da verdade.
 *
 * Gerado a partir de um snapshot do Strapi por scripts/snapshot-to-content.mjs.
 * A partir daqui é editado à mão: o tsc valida contra os tipos de lib/types.ts.
 */

export const blogPosts: BlogPost[] = [];
