# Supabase — site content + blog (src/lib/content.ts). Ver README > Supabase.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Só para `npm run seed:supabase` — nunca importar essa key no código do app.
SUPABASE_SERVICE_ROLE_KEY=
